
## 更新角色

**接口地址**:`/api/system/roles/update`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>更新角色信息</p>

**请求示例**:

```javascript
{
  "id": 1,
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|updateRoleRequest|更新角色请求|body|true|UpdateRoleRequest|UpdateRoleRequest|
|&emsp;&emsp;id|角色ID||true|integer(int64)||
|&emsp;&emsp;name|角色名称||false|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建角色

**接口地址**:`/api/system/roles/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>新建角色</p>

**请求示例**:

```javascript
{
  "code": "ROLE_ADMIN",
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createRoleRequest|创建角色请求|body|true|CreateRoleRequest|CreateRoleRequest|
|&emsp;&emsp;code|角色编码||true|string||
|&emsp;&emsp;name|角色名称||true|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建角色

**接口地址**:`/api/system/roles/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>新建角色</p>

**请求示例**:

```javascript
{
  "code": "ROLE_ADMIN",
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createRoleRequest|创建角色请求|body|true|CreateRoleRequest|CreateRoleRequest|
|&emsp;&emsp;code|角色编码||true|string||
|&emsp;&emsp;name|角色名称||true|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建角色

**接口地址**:`/api/system/roles/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>新建角色</p>

**请求示例**:

```javascript
{
  "code": "ROLE_ADMIN",
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createRoleRequest|创建角色请求|body|true|CreateRoleRequest|CreateRoleRequest|
|&emsp;&emsp;code|角色编码||true|string||
|&emsp;&emsp;name|角色名称||true|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建角色

**接口地址**:`/api/system/roles/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>新建角色</p>

**请求示例**:

```javascript
{
  "code": "ROLE_ADMIN",
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createRoleRequest|创建角色请求|body|true|CreateRoleRequest|CreateRoleRequest|
|&emsp;&emsp;code|角色编码||true|string||
|&emsp;&emsp;name|角色名称||true|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建角色

**接口地址**:`/api/system/roles/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>新建角色</p>

**请求示例**:

```javascript
{
  "code": "ROLE_ADMIN",
  "name": "管理员",
  "description": "系统管理员角色"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createRoleRequest|创建角色请求|body|true|CreateRoleRequest|CreateRoleRequest|
|&emsp;&emsp;code|角色编码||true|string||
|&emsp;&emsp;name|角色名称||true|string||
|&emsp;&emsp;description|角色描述||false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```
