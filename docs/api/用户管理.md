
## 更新用户

**接口地址**:`/api/system/users/update`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>更新用户信息</p>

**请求示例**:

```javascript
{
  "id": 1,
  "email": "user@vfdata.com",
  "phone": "13800138000",
  "enabled": true
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|updateUserRequest|更新用户请求|body|true|UpdateUserRequest|UpdateUserRequest|
|&emsp;&emsp;id|用户ID||true|integer(int64)||
|&emsp;&emsp;email|邮箱||false|string||
|&emsp;&emsp;phone|手机号||false|string||
|&emsp;&emsp;enabled|是否启用||false|boolean||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```



## 重置密码

**接口地址**:`/api/system/users/resetPassword`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>重置用户密码</p>

**请求示例**:

```javascript
{
  "userId": 1,
  "newPassword": "newPassword123"
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|resetPasswordRequest|重置密码请求|body|true|ResetPasswordRequest|ResetPasswordRequest|
|&emsp;&emsp;userId|用户ID||true|integer(int64)||
|&emsp;&emsp;newPassword|新密码||true|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 创建用户

**接口地址**:`/api/system/users/create`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>创建新用户或禁用用户</p>

**请求示例**:

```javascript
{
  "username": "admin",
  "password": "123456",
  "email": "admin@vfdata.com",
  "phone": "13800138000",
  "roleIds": [
    1,
    2
  ]
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|createUserRequest|创建用户请求|body|true|CreateUserRequest|CreateUserRequest|
|&emsp;&emsp;username|用户名||true|string||
|&emsp;&emsp;password|密码||true|string||
|&emsp;&emsp;email|邮箱||false|string||
|&emsp;&emsp;phone|手机号||false|string||
|&emsp;&emsp;roleIds|角色ID集合||false|array|integer(int64)|

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 批量导入用户

**接口地址**:`/api/system/users/batchImport`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>批量导入用户数据</p>

**请求示例**:

```javascript
{
  "users": [
    {
      "username": "admin",
      "password": "123456",
      "email": "admin@vfdata.com",
      "phone": "13800138000",
      "roleIds": "[1,2]"
    }
  ]
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|batchImportRequest|批量导入用户请求|body|true|BatchImportRequest|BatchImportRequest|
|&emsp;&emsp;users|创建用户请求||true|array|CreateUserRequest|
|&emsp;&emsp;&emsp;&emsp;username|用户名||true|string||
|&emsp;&emsp;&emsp;&emsp;password|密码||true|string||
|&emsp;&emsp;&emsp;&emsp;email|邮箱||false|string||
|&emsp;&emsp;&emsp;&emsp;phone|手机号||false|string||
|&emsp;&emsp;&emsp;&emsp;roleIds|角色ID集合||false|array|integer|

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 分配角色

**接口地址**:`/api/system/users/assignRoles`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:<p>为用户分配角色</p>

**请求示例**:

```javascript
{
  "userId": 1,
  "roleIds": [
    1,
    2,
    3
  ]
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|assignRolesRequest|分配角色请求|body|true|AssignRolesRequest|AssignRolesRequest|
|&emsp;&emsp;userId|用户ID||true|integer(int64)||
|&emsp;&emsp;roleIds|角色ID集合||true|array|integer(int64)|

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```

## 用户列表

**接口地址**:`/api/system/users/list`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:<p>分页查询用户列表</p>

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|page|页码（从0开始）|query|false|integer(int32)||
|size||query|false|integer(int32)||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultObject|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||object||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {}
}
```
