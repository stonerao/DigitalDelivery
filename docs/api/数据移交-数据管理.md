<!--
 * @Date: 2026-03-03 15:04:05
 * @LastEditors: stonerao 674656681@qq.com
 * @LastEditTime: 2026-03-05 23:12:24
 * @FilePath: \DigitalDelivery\docs\api\数据移交-数据管理.md
-->

## 数据管理页面对接接口及说明

数据管理页面（`src/views/handover/data/index.vue`）已完成与后端接口联调，页面当前使用的接口如下：

1. 页面初始化：调用 `/api/handover/kks/list` 获取台账分页列表，同时调用 `/api/handover/kks/versions` 获取版本记录。
2. 列表筛选：关键词、状态、系统节点、分页参数透传 `/api/handover/kks/list`，用于搜索和翻页。
3. 节点下拉：调用 `/api/handover/systemNodes/options` 获取系统节点选项，用于 `nodeId` 筛选。
4. 导入数据：调用 `/api/handover/kks/import` 提交导入文件，获取 `taskId` 后轮询 `/api/handover/kks/import/task`，完成后刷新列表与版本记录。
5. 数据校验：调用 `/api/handover/kks/validateAll` 发起全量校验，获取 `taskId` 后轮询 `/api/handover/kks/validate/task`，完成后刷新列表。
6. 导出台账：调用 `/api/handover/kks/export` 获取签名下载地址，前端根据返回 `url` 直接触发下载。

状态显示映射：后端返回 `VALID/SUCCESS` 映射为“已校验”，`INVALID/FAILED` 映射为“异常”，其余状态统一展示为“待校验”。

## 单条校验

**接口地址**:`/api/handover/kks/validateOne`

**请求方式**:`POST`

**请求数据类型**:`application/x-www-form-urlencoded,application/json`

**响应数据类型**:`*/*`

**接口描述**:

**请求示例**:

```javascript
{
  "kks": ""
}
```

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|kksValidateRequest|KKS校验请求|body|true|KksValidateRequest|KksValidateRequest|
|&emsp;&emsp;kks|KKS编码||true|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksValidateTaskResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksValidateTaskResponse|KksValidateTaskResponse|
|&emsp;&emsp;taskId||string||
|&emsp;&emsp;status||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"taskId": "",
		"status": ""
	}
}
```

## 导入台账数据

**接口地址**:`/api/handover/kks/import`

**请求方式**:`POST`

**请求数据类型**:`multipart/form-data`

**响应数据类型**:`*/*`

**接口描述**:

**请求示例**:

```javascript
const formData = new FormData();
files.forEach(file => {
  formData.append("files", file);
});
formData.append("importMode", "merge");
formData.append("note", "测试导入数据");
```

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|files|导入文件，支持多个|formData|true|file array||
|importMode|导入模式(merge/replace)|formData|false|string||
|note|导入说明|formData|false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksImportTaskResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksImportTaskResponse|KksImportTaskResponse|
|&emsp;&emsp;taskId||string||
|&emsp;&emsp;version||KksVersionResponse|KksVersionResponse|
|&emsp;&emsp;&emsp;&emsp;id||string||
|&emsp;&emsp;&emsp;&emsp;name||string||
|&emsp;&emsp;&emsp;&emsp;time||string||
|&emsp;&emsp;&emsp;&emsp;note||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"taskId": "",
		"version": {
			"id": "",
			"name": "",
			"time": "",
			"note": ""
		}
	}
}
```

## 数据版本记录

**接口地址**:`/api/handover/kks/versions`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:

**请求参数**:

**请求参数**:

暂无

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksVersionListResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksVersionListResponse|KksVersionListResponse|
|&emsp;&emsp;records||array|KksVersionRecord|
|&emsp;&emsp;&emsp;&emsp;id||string||
|&emsp;&emsp;&emsp;&emsp;name||string||
|&emsp;&emsp;&emsp;&emsp;time||string||
|&emsp;&emsp;&emsp;&emsp;note||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"records": [
			{
				"id": "",
				"name": "",
				"time": "",
				"note": ""
			}
		]
	}
}
```

## 校验任务状态

**接口地址**:`/api/handover/kks/validate/task`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|taskId||query|true|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksValidateStatusResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksValidateStatusResponse|KksValidateStatusResponse|
|&emsp;&emsp;taskId||string||
|&emsp;&emsp;status||string||
|&emsp;&emsp;updated||integer(int32)||
|&emsp;&emsp;abnormal||integer(int32)||
|&emsp;&emsp;reportId||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"taskId": "",
		"status": "",
		"updated": 0,
		"abnormal": 0,
		"reportId": ""
	}
}
```

## 台账数据列表

**接口地址**:`/api/handover/kks/list`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|keyword|关键词搜索|query|false|string||
|status|状态|query|false|string||
|nodeId|系统节点ID|query|false|string||
|page|页码（从0开始）|query|false|string||
|size|每页数量|query|false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultPageResultKksCode|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||PageResultKksCode|PageResultKksCode|
|&emsp;&emsp;records||array|KksCode|
|&emsp;&emsp;&emsp;&emsp;id||string||
|&emsp;&emsp;&emsp;&emsp;createdAt||string||
|&emsp;&emsp;&emsp;&emsp;updatedAt||string||
|&emsp;&emsp;&emsp;&emsp;createdBy||string||
|&emsp;&emsp;&emsp;&emsp;updatedBy||string||
|&emsp;&emsp;&emsp;&emsp;kks||string||
|&emsp;&emsp;&emsp;&emsp;name||string||
|&emsp;&emsp;&emsp;&emsp;type||string||
|&emsp;&emsp;&emsp;&emsp;status||string||
|&emsp;&emsp;&emsp;&emsp;category||string||
|&emsp;&emsp;&emsp;&emsp;systemNodeId||string||
|&emsp;&emsp;total||integer(int64)||
|&emsp;&emsp;page||integer(int32)||
|&emsp;&emsp;size||integer(int32)||
|&emsp;&emsp;totalPages||integer(int32)||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"records": [
			{
				"id": "",
				"createdAt": "",
				"updatedAt": "",
				"createdBy": "",
				"updatedBy": "",
				"kks": "",
				"name": "",
				"type": "",
				"status": "",
				"category": "",
				"systemNodeId": ""
			}
		],
		"total": 0,
		"page": 0,
		"size": 0,
		"totalPages": 0
	}
}
```

## 导入任务状态

**接口地址**:`/api/handover/kks/import/task`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|taskId||query|true|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksImportStatusResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksImportStatusResponse|KksImportStatusResponse|
|&emsp;&emsp;taskId||string||
|&emsp;&emsp;status||string||
|&emsp;&emsp;total||integer(int32)||
|&emsp;&emsp;inserted||integer(int32)||
|&emsp;&emsp;updated||integer(int32)||
|&emsp;&emsp;failed||integer(int32)||
|&emsp;&emsp;errorFileUrl||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"taskId": "",
		"status": "",
		"total": 0,
		"inserted": 0,
		"updated": 0,
		"failed": 0,
		"errorFileUrl": ""
	}
}
```

## 导出台账

**接口地址**:`/api/handover/kks/export`

**请求方式**:`GET`

**请求数据类型**:`application/x-www-form-urlencoded`

**响应数据类型**:`*/*`

**接口描述**:

**请求参数**:

**请求参数**:

| 参数名称 | 参数说明 | 请求类型    | 是否必须 | 数据类型 | schema |
| -------- | -------- | ----- | -------- | -------- | ------ |
|keyword|关键词搜索|query|false|string||
|status|状态|query|false|string||
|nodeId|系统节点ID|query|false|string||
|page|页码（从0开始）|query|false|string||
|size|每页数量|query|false|string||

**响应状态**:

| 状态码 | 说明 | schema |
| -------- | -------- | ----- |
|200|OK|ResultKksExportResponse|

**响应参数**:

| 参数名称 | 参数说明 | 类型 | schema |
| -------- | -------- | ----- |----- |
|code||integer(int32)|integer(int32)|
|message||string||
|data||KksExportResponse|KksExportResponse|
|&emsp;&emsp;url||string||
|&emsp;&emsp;signed||boolean||
|&emsp;&emsp;auditLogId||string||
|&emsp;&emsp;expireAt||string||

**响应示例**:

```javascript
{
	"code": 0,
	"message": "",
	"data": {
		"url": "",
		"signed": true,
		"auditLogId": "",
		"expireAt": ""
	}
}
```
